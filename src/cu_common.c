#include "cu_common.h"

bool is_text_file(const char *path);
bool is_folder(const char *path);

/* chec if text file */
bool is_text_file(const char *path)
{
    return false;
}

/* check if folder */
bool is_folder(const char *path)
{
    return false;
}
